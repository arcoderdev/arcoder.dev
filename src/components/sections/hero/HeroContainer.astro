---
interface Props {
    className?: string;
}

const { className = "" } = Astro.props;
---

<section id="hero" class="w-full h-full relative">
    <div class:list={["p-6 2xl:px-0 w-full max-w-360 mx-auto", className]}>
        <slot />
    </div>
</section>
